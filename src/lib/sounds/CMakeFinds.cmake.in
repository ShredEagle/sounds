@find_package@(Graphics CONFIG @REQUIRED@ COMPONENTS handy platform)
@find_package@(Math CONFIG @REQUIRED@ COMPONENTS math)

@find_package@(OpenAL @REQUIRED@ CONFIG)
@find_package@(Vorbis @REQUIRED@ File CONFIG)
